
<div class="row"> 
  <div class="col s12">
    <h3>Search for a Venue</h3>
  </div>
  <div class="col s12"><% @events.each do |event| %>
    <div class="card">
      <div class="card-content"><span class="card-title"><%= event.name %></span></div>
      <div class="card-action"></div>
    </div><% end %>
  </div>
</div>